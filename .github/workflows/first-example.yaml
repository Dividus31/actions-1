name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubunti-latest
        steps:
        - name: Welcome message
            run: echo "My first GitHub Actions Job"
        - name: List files
            run: ls
        - name: Read File
            run: cat README.md